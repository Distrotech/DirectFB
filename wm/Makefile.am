## Makefile.am for DirectFB/wm

if ENABLE_DEFAULT
DEFAULT_DIR = default
else
DEFAULT_DIR =
endif

if ENABLE_ARIB
ARIB_DIR = arib
else
ARIB_DIR =
endif

if ENABLE_UNIQUE
UNIQUE_DIR = unique
else
UNIQUE_DIR =
endif

SUBDIRS = $(DEFAULT_DIR) $(ARIB_DIR) $(UNIQUE_DIR)
